import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GoQuant Orderbook Simulator | Real-Time Trading Analytics",
  description: "Professional-grade real-time orderbook viewer and order simulation platform supporting OKX, Bybit, and Deribit exchanges. Built for traders who demand precision.",
  keywords: ["orderbook", "trading", "cryptocurrency", "real-time", "OKX", "Bybit", "Deribit", "market depth"],
  authors: [{ name: "Naman Bhatt" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased overflow-x-hidden`}
      >
        {children}
      </body>
    </html>
  );
}
